# Public artwork

- `../../plugins/codex-image-context-runtime/assets/logo.svg` is the original project and Codex plugin logo. The brackets represent bounded context, the image frame represents generation and inspection, the short lines represent bounded text results, and the side node represents the durable local job runtime.
- `github-social-preview-source.svg` is the editable source for the repository social preview.
- `github-social-preview.png` is the 1280 × 640 GitHub social preview rendered from that source.
- `image-context-runtime-workstation-comparison.png` is the primary README workflow image and uses a general website-assets and screenshot-research scenario.

The logo and social preview are original project branding and do not reproduce an OpenAI or Codex product mark. The workstation comparison is fictional concept artwork: it does not depict an actual Codex interface and is not benchmark evidence.
